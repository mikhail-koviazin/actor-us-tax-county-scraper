# This file tells Git which files shouldn't be added to source control

.DS_Store
.idea
.vscode
.zed
node_modules
storage

# Added by Apify CLI
.venv

# The Travis index: 368 MB of derived data, rebuilt by bin/build-travis-index.js
.travis-index

# Sweep artifacts: the identifier pools are cached to avoid re-downloading a layer, and the
# reports belong in the research repository under data/, not here.
.sweep-cache
sweep-report*.json
